const Main = ({
  title = 'Olá Mundo!',
  description = 'TypeScript, ReactJS, NetxJS e Styled Components'
}) => (
  <main>
    <h1>{title}</h1>
    <p>{description}</p>
  </main>
)

export default Main
